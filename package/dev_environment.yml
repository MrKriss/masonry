name: masonry
channels:
- defaults
- conda-forge
dependencies:
  # Core
  - python=3.6.1

  # 3rd party
  - cookiecutter
  - docopt
  - gitpython
  - clint
  - flake8

  # Testing 
  - pytest
  - pytest-cov
  - coverage

  - pip:
    - inquirer
    - ruamel.yaml
    - schema
    - pytest-watch
    - autopep8
